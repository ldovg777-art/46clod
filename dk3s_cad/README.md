# Чертёж датчика концентрации ДК-3С-210АВ на AutoLISP (nanoCAD)

`dk3s_drawing.lsp` строит чертёж общего вида датчика ДК-3С-210АВ: формат А2, масштаб 1:2,
рамка и основная надпись по ГОСТ 2.104 (форма 1), главный вид с размерами и выносками,
выносной элемент А (2:1) с узлом рабочего электрода, изометрия с вырезом четверти (ГОСТ 2.317),
технические требования.

Результат прогона через проверочный интерпретатор (без CAD): `dk3s_drawing.dxf`, `dk3s_preview.png` и
`dk3s_drawing.dwg`. Размеры в них построены ODA File Converter — движком, на котором сделан nanoCAD, поэтому
размерные числа стоят так же, как их поставит nanoCAD (см. «Размеры как в nanoCAD»).
`src/dk3s_drawing_readable.lsp` - тот же исходник с читаемой кириллицей (для правки; в nanoCAD грузить `dk3s_drawing.lsp`,
он получается из читаемого конвертером `tools/lisp_cyr_escape.py`). `sync_from_github.bat` - копирует папку и навык
`.claude/skills/nanocad-lisp` из GitHub в локальную папку (например `G:\Мой диск\AutoCAD`).

## Запуск в nanoCAD

Интерактивно:

1. Открыть новый документ (шаблон с миллиметрами).
2. Команда `APPLOAD` (или `_.APPLOAD`), выбрать `dk3s_drawing.lsp`. Альтернатива в командной строке:
   `(load "C:/путь/к/dk3s_drawing.lsp")`.
3. Команда `DK3S`. В командной строке появится `DK3S: drawing complete, entities: 1879, dims: entmake, ...`.
4. Сохранить как DWG.

Пакетно (без окна): `dk3s_build.bat` ищет `nCad.exe` в `%ProgramFiles%\Nanosoft\*` и запускает
`nCad.exe -invisible -s dk3s_build.scr`. Скрипт грузит LISP, выполняет `DK3S`, сохраняет `dk3s_drawing.dwg`
и закрывает nanoCAD. Подсказки команды `SAVEAS` в разных версиях nanoCAD различаются: если DWG не создался,
поправьте две строки после `_.SAVEAS` в `dk3s_build.scr` или сохраните интерактивно.

Код совместим с AutoCAD, BricsCAD, ZWCAD: только `entmake`, без ActiveX и DCL.

## Оформление как в чертежах ЭКОР

Толщины линий, стили и рамка сняты с DXF деталей ЭКОР (`ref/ekor_dxf/`): контур 0,6 мм (слой «Корпус»),
тонкие линии 0,3 (внутренний диаметр резьбы, штриховка, строки штампа), рамка и штамп 0,6, размеры и текст 0,25.
Слои: `DK3S_CONTOUR` 0,6; `DK3S_THIN` 0,3; `DK3S_AXIS` 0,3 (тип линии как «ГОСТ 2.303 5»: 20/1,5/1/1,5);
`DK3S_DIM` и `DK3S_TEXT` 0,25; `DK3S_FRAME` 0,6. Значения в параметрах `g_dk3s_lw_*` (сотые мм).
Размерный стиль как GOSTMTM/ЕСКД: стрелки 3,0, выносные 1,25, отступ и зазор 0,625, текст 3,5 над линией, запятая.
Шрифт `GOST.shx` с наклоном 15° (стиль GOST в ваших чертежах). В поставке nanoCAD его нет — о замене см. раздел «Кириллица».
Рамка: дополнительные графы слева (Инв. № подл., Подп. и дата, Взам. инв. №, Инв. № дубл., Подп. и дата, Справ. №,
Перв. примен.), графа обозначения 70x14 на верхней рамке, «Копировал» и «Формат А3» под штампом.

## Что изменено в версии 1.3.2 (19.09.2026, по замечанию Леонида)

- **Узел уплотнения без зазоров:** убраны технологические зазоры 0,3 мм модели — конус буртика фторопластовой втулки
  лежит на седле корпуса, торец буртика упирается в грундбуксу (в узле зазоров нет). Проверено в FreeCAD: втулка,
  грундбукса, гровер и гайка касаются (расстояние 0), пересечений нет. Буртик вышел 29,9 мм при 30 по чертежу
  715412.001 — с гровером 4,5 пакет «седло — втулка — грундбукса — гровер — гайка» сходится с положением гайки.
- Раздел 11 пересобран: `entities: 1879` (ушли двойные линии зазоров), размеров 21, проверка надписей — 0 нарушений.

## Что изменено в версии 1.3.1 (19.09.2026, по просьбе Леонида)

- **Гровер по действующему ГОСТ 6402-70:** шайба 20 нормальная — b = s = **4,5** ±0,15 (было 5,0: это размер «в скобках»,
  по примечанию к табл. 1 допускался только до 01.01.85). Сверено по официальному изданию стандарта и текстовой копии;
  PDF лежит в `Мой диск\AutoCAD\Нормативные_документы\`. В модели (`tools/iso/sensor_model.py`, `GROVER_S/B`) гайка
  осталась на месте по общему виду, грундбукса подошла к ней на 0,5 мм, буртик втулки на 0,5 мм длиннее.
- Раздел 11 (изометрия) пересобран из модели; остальное без изменений: `entities: 1885`, размеров 21, проверка
  надписей — 0 нарушений (и в строгом режиме). Отдельный лист изометрии и STEP в папке AutoCAD тоже пересобраны.

## Что изменено в версии 1.3.0 (19.09.2026, по просьбе Леонида)

- **Лист А2 (594x420)** вместо А3: на А3 изометрия 1:2 (около 270x175 мм) не помещалась. Компоновка задана
  параметрами `g_dk3s_main_*`, `g_dk3s_det_*`, `g_dk3s_iso_*`, `g_dk3s_notes_*` (мм бумаги): вверху главный вид и
  справа элемент А, внизу изометрия, справа над основной надписью технические требования; «Формат» — `g_dk3s_format`.
- **Изометрия с вырезом** (раздел 11, функция `dk3s_draw_iso`): приведённая по ГОСТ 2.317, вырезана четверть
  гильзы, корпуса, грундбуксы, гровера, гайки нажимной и фторопластовой втулки; штриховка разрезов по ГОСТ 2.317,
  фторопласт «в клетку». Слой `DK3S_ISO` (контур 0,5), штриховка — `DK3S_THIN`, выноски — `dk3s_leader`, как на
  главном виде (детали, подписанные на главном виде, на изометрии не повторяются). Раздел **генерируется** из
  3D-модели FreeCAD — руками не править (см. «Изометрия»).
- Технические требования: п. 8 — втулка 715412.001 (Ф-4Д) и гровер (шайба 20 65Г ГОСТ 6402-70) между грундбуксой
  и гайкой нажимной; п. 9 — изометрия, трубки внутри гильзы показаны условно.
- `run_dk3s.py`: превью — по габариту листа (было жёстко под А3). Итог команды: `entities: 1885`, размеров 21.

### Изометрия

Модель и проекция — `tools/iso/`: `sensor_model.py` (FreeCAD Part, размеры — из таблицы параметров и чертежей ЭКОР,
втулка — по 715412.001: 3 канала Ø6 на Ø11), `sensor_iso_fc.py` (вырез, удаление невидимых линий, штриховка),
`build_iso_lsp.py` (раздел LISP), `update_iso_section.py` (вставка между метками ISO BEGIN/END),
`build_sensor_iso_dxf.py` (отдельный лист изометрии в DWG), `sensor_labels.json` (выноски: точка модели, излом).
Нужен FreeCAD 1.1 (`freecadcmd`). Порядок пересборки — в начале `update_iso_section.py`. Внутри гильзы трубки и концы
мостиков показаны условно: разреза датчика в техописании K1 нет.

## Что изменено в версии 1.2.1 (19.09.2026, по замечанию Леонида)

- **Электролитические мостики — трубки Ø6**, как трубка РЭ (было Ø3,2). На монтажном чертеже K1 (лист 16) на
  развилке у электродов сравнения мостики той же толщины, что трубка РЭ; в жгуте три трубки лежат треугольником:
  РЭ спереди, мостики за ней, поэтому на виде от каждого мостика видна полоса 3 мм и контур жгута ±6
  (`g_dk3s_bridge_d` 6, `g_dk3s_bridge_r` 3). Прежние Ø3,2 были сняты с этой видимой полосы.
- Внутренняя образующая S-изгиба мостика у начала закрыта трубкой и разъёмом РЭ — рисуется с выхода из-за
  разъёма (`dk3s_draw_bend`, новый параметр `rh`).
- Выноска «Электролитический мостик» указывает на мостик перед хомутом (z 466; точка z 470 была внутри хомута).

## Что изменено в версии 1.2 (19.09.2026, по замечаниям Леонида)

- **Узел рабочего электрода (элемент А) — по Fig. 4/5 «Технического описания K1» (стр. 48–49).** Наконечник
  фторопластовый: хвостовик Ø5 длиной 15 под защитную трубку (трубка надета на него с натягом), тело Ø7
  длиной 18,3 (цилиндр 12,5 + шестигранник 1/4" 5,8), конус 3,5 к носику Ø1,8. Наконечник накручен на
  токоотвод (резьба M3x0,5), токоотвод на конце конусом переходит в рабочий электрод Ø1,4 (выступ 2).
  Трубка и наконечник заштрихованы «в клетку» (ГОСТ 2.306, неметаллы), смежные детали — разным шагом
  (`dk3s_hatch_cross`). Размеры наконечника сняты с рисунков техописания (около 15 пикс./мм, сходятся
  с Ø6/Ø5/Ø7/Ø3/Ø1,4) — при появлении чертежа наконечника уточнить в таблице параметров (`g_dk3s_tip_*`, `g_dk3s_tap_*`).
- **Размеры — как их ставит nanoCAD.** Прогон через ODA показал, что проверочный рендер (ezdxf) ставил числа
  не так, как CAD: вертикальные — справа от линии (nanoCAD и AutoCAD — всегда слева), невлезающие — по центру
  (nanoCAD при DIMTIX 0 уводил их на выноске, на соседние надписи). Стиль теперь: DIMTIX 1 (число между
  выносными), DIMTOFL 1 (линия между выносными всегда, ГОСТ 2.307), DIMTMOVE 0 (число прикреплено к линии).
- **Числа, которые между выносными легли бы на линии, вынесены за контур** на продолжение размерной линии
  (ГОСТ 2.307, рис. 14): `dk3s_dim_t` с точкой `dk3s_dimtxt_v` / `dk3s_dimtxt_h` (группа 70 с флагом 128 и
  группа 11). Так стоят Ø6 трубки (над мостиком), Ø3 провода ВЭ (под проводом), Ø20 (над электродом),
  25,5 (левее скобы), в элементе А — Ø6, Ø7, Ø1,4. Размер 61,1 перенесён в промежуток между электродами.
  Осевая прервана под числами Ø32 и M42x3 (`dk3s_axis_seg`; ГОСТ 2.307: осевые под числом прерывают).
- Итог команды: `entities: 757`, размеров 21.

### Размеры как в nanoCAD (ODA)

`run_dk3s.py` после интерпретатора убирает из DXF блоки размеров и прогоняет его через ODA File Converter
(DXF -> DWG с аудитом -> DXF): ODA строит размеры заново своим движком. Ищется в `%ProgramFiles%\ODA\ODAFileConverter*`,
путь можно задать `--oda путь` или переменной `ODA_CONVERTER`; `--oda off` — без него (тогда размеры рисует ezdxf,
положение чисел может отличаться от nanoCAD, о чём печатается предупреждение). Проверенное поведение ODA
(19.09.2026): число вертикального размера — слева от линии; вынесенное число при DIMTMOVE 0 CAD отодвигает от контура
на длину стрелки (при наружных стрелках — ещё и на её хвост) и продлевает линию до него; при DIMTMOVE 1 линия
продлевается, только если число далеко, иначе оно съезжает вбок без линии.

## Что изменено в версии 1.1 (19.09.2026, по замечаниям Леонида)

- **Шестигранники S46 (корпус) и S36 (гайка) — как у ЭКОР**: вид по углам, три грани, фаска 30° до размера
  под ключ, на каждой грани дуга (`dk3s_hex`, `dk3s_arc3`). Глубина фаски у рёбер c = (e − s)/2·tg30°, дуга
  через концы рёбер и середину грани на торце; на гайке 714.541.000 совпадает с дугами ЭКОР до 0,02 мм.
  Размер по углам — с чертежей ЭКОР: `g_dk3s_hex1_e` 52 (корпус), `g_dk3s_hex2_e` 41 (гайка).
- **Размеры корпуса и гайки (Ø59, S46, M33x2, S36, Ø20,8) с общего вида убраны** — они на чертежах деталей
  714.761.000 и 714.541.000; на общем виде остались габаритные и присоединительные.
- **Основная надпись — по форме 1 ГОСТ 2.104**: Лит./Масса/Масштаб 15/17/18, строка значений 15 мм,
  Лист/Листов 20/30, графа организации 50x15 (в две строки: `g_dk3s_org1`, `g_dk3s_org2`).
  Подписи граф 3 мм; если не входят в графу (с зазором 1 мм) — сжатие по ширине, как в штампах ЭКОР.
- **Ширина надписей считается по ГОСТ 2.304 тип Б** (`dk3s_text_w`: прописная 0,8h, строчная и цифра 0,7h,
  пробел 0,6h, узкие знаки 0,45h, плюс наклон 0,27h) с запасом `g_dk3s_font_k` = 1,08 — на GOST.shx, CS_Gost2304.shx
  и подмену шрифта. Раньше было 0,6h — при настоящем шрифте ГОСТ строки вылезали за рамку, выноски пересекали надписи.
- **Технические требования переносятся по словам** до рамки, продолжение — с отступом под номером; блок стоит
  на 8 мм выше основной надписи.
- **Выноски разложены перебором**: надписи не пересекаются ни с чем, выноски не пересекают друг друга, полки,
  размеры и надписи; полка выступает за выноску на 0,3h, надпись с полкой влево выровнена по правому краю.
  Линии деталей пересекают только выноски наконечника (изнутри скобы) и токоотвода РЭ (под мостиками).
- Осевая прервана на бирке (не проходит через надпись). Итог команды: `entities: 478`, размеров 21.

### Проверка надписей

`python tools/lispcheck/check_text_fit.py dk3s_drawing.dxf [--margin -0.15]` — каждая надпись как прямоугольник
по той же модели ширины (с учётом сжатия, поворота, выравнивания) против всех линий, дуг и других надписей.
С версии 1.2 проверяются и размерные числа (из блоков размеров, как их построил CAD) — против линий, надписей
и линий других размеров. Код 0 — нарушений нет. `--margin -0.15` — строгий режим с запасом 0,15h вокруг
надписи (кроме низа, где полка). Версия 1.0 давала 17 нарушений надписей, 1.1 — 0, но размерные числа тогда
не проверялись: у 1.1.1 на листе с размерами ODA их 8 (числа на осевой, на линиях трубки, мостика и
электрода, «25,5» под выносной). Версия 1.2 — 0 (и в строгом режиме). Касание двух размерных линий проверка
не ловит — это смотреть глазами по PNG.

## Как устроен файл

| Раздел | Что содержит |
|---|---|
| 1. Таблица параметров | все размеры датчика и листа в `setq`-переменных `g_dk3s_*`, мм |
| 2. Служебные переменные | имена слоёв, стилей, текущее начало и масштаб вида |
| 3. Примитивы | `dk3s_line`, `dk3s_arc`, `dk3s_pline`, `dk3s_text`, `dk3s_ellipse`, штриховка линиями |
| 4. Таблицы | слои, тип линии осевой, текстовый стиль, размерный стиль |
| 5. Размеры и выноски | `dk3s_dim` (entmake DIMENSION, при отказе - команда DIMLINEAR), `dk3s_leader` (полка по ширине надписи) |
| 6. Рамка и штамп | А3, поля 20/5, форма 1 (185x55) по ГОСТ 2.104, подгонка надписей `dk3s_tb_fit` |
| 7. Главный вид | скоба, наконечник, гильза с перфорацией, корпус M42x3, фланец, S46/S36, бирка, мостики, электроды сравнения |
| 8. Выносной элемент А | по Fig. 4: трубка Ø6 на хвостовике наконечника, наконечник Ø7 (фторопласт, «в клетку»), токоотвод M3x0,5 с конусом, электрод Ø1,4 с выступом 2 мм |
| 9. Размеры и ТТ | размерные цепи главного вида, выноски, технические требования |
| 10. Команда `DK3S` | стадии INIT, TABLES, FRAME, MAIN, DIMS, LABELS, DETAIL, ISO, NOTES, DONE; при ошибке печатается стадия |
| 11. Изометрия | `dk3s_draw_iso` — генерируется `tools/iso/build_iso_lsp.py` из модели FreeCAD |

Всё строится в пространстве модели: датчик в масштабе 1:1, рамка и надписи увеличены в 2 раза
(`g_dk3s_scale_den`). Печать листа с масштабом 1:2 даёт А2. Размеры показывают истинные значения.

Правка размеров: только раздел 1. Осевые станции заданы абсолютно (`g_dk3s_z_*`, от вершины скобы),
диаметры отдельно. Обозначение документа и фамилии в штампе: `g_dk3s_designation`, `g_dk3s_author`, `g_dk3s_checker`.

## Кириллица

Русские строки в `.lsp` записаны кодами `\\U+XXXX`: так текст верно рисуется независимо от кодировки файла
и локали Windows (nanoCAD читает `.lsp` в ANSI, cp1251). Комментарии в UTF-8, на выполнение не влияют.
Чтобы править тексты: `python3 tools/lisp_cyr_escape.py --decode dk3s_drawing.lsp > src.lsp`,
отредактировать, затем `python3 tools/lisp_cyr_escape.py src.lsp > dk3s_drawing.lsp`.

Шрифт надписей `GOST.shx` с наклоном 15° (`g_dk3s_font`), как в чертежах ЭКОР. В поставку nanoCAD он не входит: свой шрифт ГОСТ
у nanoCAD — `CS_Gost2304.shx` (ещё `CS_Gost2304A.shx` и `GOST 2.304 type A.ttf`), SHX-шрифты лежат в
`C:\ProgramData\Nanosoft\nanoCAD XX\SHX`, отсутствующий шрифт nanoCAD сам заменяет на `CS_Gost2304.shx`
(по справке nanocad.com; на живой установке не проверено). Два пути: скопировать `GOST.shx` из шрифтов
AutoCAD ЭКОР в эту папку SHX (вид как у ЭКОР) либо поставить `g_dk3s_font "CS_Gost2304.shx"`. `ISOCPEUR.ttf` —
шрифт AutoCAD, в списке шрифтов nanoCAD его нет. Строка `text style: DK3S_GOST` в итоге команды наличие файла
шрифта НЕ подтверждает: стиль создаётся и с отсутствующим шрифтом, проверять глазами по надписям.

## Запасные пути (на случай отличий LISP в nanoCAD)

- `entmake` слоя не прошёл - слой создаётся командой `-LAYER`.
- `entmake` типа линии не прошёл - осевые рисуются сплошными.
- `entmake` текстового стиля не прошёл - используется `Standard`.
- `entmake` размерного стиля или размера не прошёл - переменные DIM* выставляются `setvar`,
  размеры ставятся командой `DIMLINEAR` (переключение автоматическое, режим печатается в итоге).

Все четыре пути проверены на интерпретаторе (`--fail DIMENSION,DIMSTYLE`, `--fail LAYER,LTYPE,STYLE`).

## Проверка без CAD

`tools/lispcheck/autolisp_mini.py` - интерпретатор подмножества AutoLISP (reader, динамическая область
видимости, defun, списки, строки, арифметика как в AutoLISP, `entmake` в ezdxf, `tblsearch`, `getvar`/`setvar`,
`command` для ZOOM, -LAYER, DIMLINEAR). `tools/lispcheck/run_dk3s.py` прогоняет чертёж и пишет DXF + PNG:

```
pip install ezdxf matplotlib
python3 tools/lispcheck/run_dk3s.py --out out            # DXF + PNG
python3 tools/lispcheck/run_dk3s.py --fail DIMENSION     # проверка запасного пути
```

Интерпретатор ловит ошибки синтаксиса, арности, неопределённые символы и логику геометрии,
но не заменяет прогон в nanoCAD: поведение `entmake` для DIMENSION/DIMSTYLE и подсказки `SAVEAS`
надо подтвердить на реальной системе один раз.

## Откуда размеры

Геометрия снята с векторного монтажного чертежа датчика в «Техническом описании K1» (лист 16 PDF):
масштаб восстановлен по размеру 411 мм, контур извлечён из PDF-геометрии с точностью около 0,1 мм.
Узел рабочего электрода - по Fig. 4/5 (стр. 48–49): электрод Ø1,4 мм, выступ 2 мм; наконечник - хвостовик Ø5x15
под трубку, тело Ø7x18,3 (шестигранник 1/4"), конус 3,5, носик Ø1,8; токоотвод M3x0,5 с конусом 13,6–18,7 от торца тела.

| Параметр | Значение | Источник |
|---|---|---|
| Общая длина (скоба - штыри электродов сравнения) | 613,8 | лист 16 |
| От вершины скобы до плоскости фланца DN50 | 207,6 (на чертеже 207,8) | лист 16, размер 207.6 |
| От плоскости фланца до штырей | 411 | лист 16, размер 411 |
| Защитная гильза | Ø32 x 222, перфорация Ø6 шаг 20 (3 + 2x2 отверстий) | лист 16 |
| Корпус 714.761.000 | M42x3-6f, длина 61 h12, 34±0,2 до фланца, фланец Ø59 h12, S46, внутри M33x2-6F x20 | чертёж корпуса (DXF) |
| Гайка нажимная 714.541.000 | S36, M33x2-6g x20, всего 30 h12, канавка Ø30x3, отверстие Ø20,8 | чертёж гайки (DXF) |
| Гнездо 713.165.001 (входной узел) | Ø61,4, Ø66, M42x3-6F, 38 (удлинённое 88) | чертёж гнезда (DXF) |
| Электроды сравнения | Ø20, длина 61,1, межосевое 45,6 | лист 16 |
| Токоотвод РЭ / мостики / провод ВЭ | Ø6 / Ø6 / Ø3 | лист 16 (мостики — развилка у ЭС), Fig. 4 |

Модель `sensor_3d_model.py` в корне репозитория (Ø20, «входной узел M42» на кабельном конце) чертежу
не соответствует и не использовалась.

Обозначение `302123.000 ВО` взято из графы «Перв. примен.» чертежей корпуса, гайки и грундбуксы (302123.000 - датчик); подтверждено Леонидом 19.09.2026.
Масса не заполнена. Материал корпуса, гайки и грундбуксы - 08Х18Н10Т ГОСТ 5949-75 (по чертежам деталей).
